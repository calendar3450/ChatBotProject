spring.application.name=project
spring.devtools.livereload.enabled=true
spring.devtools.restart.enabled=true

# H2 Console 활성화
spring.h2.console.enabled=true

# (선택 사항) H2 접속 경로 설정 (기본값: /h2-console)
spring.h2.console.path=/h2-console

# 데이터베이스 접속 정보
# spring.datasource.url=jdbc:h2:~/docchat;MODE=MySQL;AUTO_SERVER=TRUE
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# spring.jpa.hibernate.ddl-auto=update

# MariaDB 연결 설정
spring.datasource.url=jdbc:mariadb://localhost:3306/chatbot_db
spring.datasource.username=root
spring.datasource.password=mjk020
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA 설정
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
# 애플리케이션 시작 시 테이블 자동 생성/업데이트 (운영 환경에서는 validate 등을 권장)
spring.jpa.hibernate.ddl-auto=update
# SQL 로그 보기 (선택 사항)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 카프카 서버 주소 (보통 로컬은 9092)
spring.kafka.bootstrap-servers=localhost:9092

# Producer 설정 (보내는 쪽)
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer

# Consumer 설정 (받는 쪽)
spring.kafka.consumer.group-id=chat-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer